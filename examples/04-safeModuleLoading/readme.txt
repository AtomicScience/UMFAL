"Safe Module Loading" example shows, how UMFAL deals with absent modules - and how to load them safely, without triggering an error if they are absent

It features an entry point - `init.lua`, and a couple of modules, that will be loaded via different ways