"Module caching" example explains, how UMFAL caches modules

It features an entry point - `init.lua`, and a single module 'module.lua', to be loaded in different ways, to show, how caching in UMFAL works